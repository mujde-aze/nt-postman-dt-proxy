export interface ActivityResponse {
    // eslint-disable-next-line camelcase
    meta_key: string;
    // eslint-disable-next-line camelcase
    object_note: string;
    // eslint-disable-next-line camelcase
    hist_time: string;
}
